
version: 0.2

phases:
  install:
    commands:
      - npm install
      - yarn install
  build:
    commands:
      - npm run build
      - yarn build
artifacts:
  files:
    - 'build/* '
    - 'appspec.yml'
